export enum COMMUNITY_TYPE {
  RESIDENTIAL = 'Residential',
  SOCIAL = 'Social'
}

export enum INVITE_STATUS {
  PENDING = 'pending',
  CHECKIN = 'checked-in',
  CHECKOUT = 'checked-out',
  REVOKED = 'revoked',
  EXPIRED = 'expired'
}
